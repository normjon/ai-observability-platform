# Terraform — never committed
**/.terraform/
**/*.tfstate
**/*.tfstate.backup
**/terraform.tfvars
**/crash.log
**/override.tf
**/.terraform.lock.hcl
**/tfplan

# Lambda build artifacts — never committed
**/lambda/package/
**/lambda/*.zip
